# ═══════════════════════════════════════════════════════════════════════
# BONOLOTO 2.0 — Dependencias backend
# Refactor estructural sobre v7.0. Compatible ARM aarch64.
# ═══════════════════════════════════════════════════════════════════════

# ── API Framework ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0

# ── Base de datos Oracle ──
oracledb==2.0.1

# ── IA y cálculo numérico (core) ──
numpy==1.26.3
scipy==1.12.0

# ── Algoritmos de ML ──
scikit-learn==1.4.0

# ── Series temporales clásicas ──
statsmodels==0.14.1

# ── Reservoir Computing (ESN) ──
reservoirpy==0.3.6

# ── TDA — Análisis Topológico ──
giotto-tda==0.6.0

# ── Regresión Simbólica ligera ──
gplearn==0.4.2

# ── Autenticación JWT ──
PyJWT==2.8.0
python-multipart==0.0.6

# ── HTTP Cliente ──
httpx==0.26.0
requests==2.31.0

# ── Utilidades ──
python-dotenv==1.0.0
pydantic==2.5.3

# ── Watchdog + Scheduler ──
watchdog==3.0.0
apscheduler==3.10.4

# ── Logging ──
loguru==0.7.2

# ── Predicción Conforme ──
nonconformist==2.1.0

# ── Bloques I, J, K ──
PyWavelets==1.6.0
scikit-fuzzy==0.4.2
causal-learn==0.1.3.6
pyts==0.13.0
dtaidistance==2.3.12
emd==0.6.2
vmdpy==0.2
